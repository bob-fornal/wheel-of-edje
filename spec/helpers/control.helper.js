
const ctrl = {};

ctrl.init = () => {};

ctrl.mock = {
  init: () => {},
  spinner: {}
};

ctrl.mock.spinner.winnerDisplay = () => {};

if (typeof module !== 'undefined' && module.hasOwnProperty('exports')) {
  module.exports = ctrl;
}
