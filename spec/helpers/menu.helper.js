
let menuHelper = {};

menuHelper.init = () => {};

menuHelper.mock = {
    clearActivePerson: () => {}
};

if (typeof module !== 'undefined' && module.hasOwnProperty('exports')) {
    module.exports = menuHelper;
}
